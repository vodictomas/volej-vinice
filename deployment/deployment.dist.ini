; Volejbálek – konfigurace nasazení (dg/ftp-deployment)
;
; Použití
;	Doplnit do řádku remote přístup k ftp
;	(parametr -t spouští testovací mód):
;	php vendor/bin/deployment deployment/deployment.ini -t

[production]

remote = ftps://UZIVATEL:HESLO@ftp.example.cz/www
passiveMode = yes

; cesta k projektu, relativně k tomuto ini souboru
local = ..

test = no

; Na serveru běží i jiné soubory (staré .htm stránky, data), které v repozitáři nejsou.
; allowDelete = yes by je při prvním nasazení smazalo. Zapni až po ověření obsahu serveru.
allowDelete = no

ignore = "
	/.git
	/.gitignore
	/.idea
	/nbproject
	/node_modules
	/old
	/deployment
	/db
	/LICENSE
	/composer.json
	/composer.lock
	/package.json
	/package-lock.json
	/webpack.config.js
	/tsconfig.json
	/phpstan.neon
	/app/config/local.neon
	/log/*
	!/log/.htaccess
	/temp/cache/*
	/temp/sessions/*
	/vendor/bin
	/vendor/dg/ftp-deployment
	/vendor/phpseclib
	/vendor/paragonie
"

before[] = local: git diff-index --quiet HEAD
before[] = local: npm run build
before[] = local: composer install --no-dev --no-interaction --optimize-autoloader

; Po nahrání smazat cache DI kontejneru a Latte, aby se regenerovala
purge[] = temp/cache

; Práva na sdíleném hostingu
after[] = remote: chmod 0777 temp/cache
after[] = remote: chmod 0777 temp/sessions
after[] = remote: chmod 0777 log

; Vrátit lokálnímu vendoru dev balíčky (phpstan, cs-fixer, composer/composer).
; Local-after joby běží i když se nic nesynchronizovalo.
after[] = local: composer install --no-interaction

deploymentFile = .htdeployment
filePermissions = 0644
dirPermissions = 0755
